import React from "react";
import { Alert } from 'reactstrap';

const ConfirmEmailMessage = () => (
  <div>
    <Alert color="info">
      'Please verify your email address to unlock the account'
    </Alert>
  </div>
);

export default ConfirmEmailMessage;
